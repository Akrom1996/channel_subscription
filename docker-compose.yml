version: '3.8'

services:
  bot-subscription:
    build: .
    container_name: bot-payment
    env_file:
      - .env
    volumes:
      - ./subscriptions.db:/app/subscriptions.db
    restart: unless-stopped
    mem_limit: 512m